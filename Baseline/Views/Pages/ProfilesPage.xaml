<UserControl x:Class="BaseLine.Views.Pages.ProfilesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Snapshots"
                   Style="{StaticResource HeaderTitleTextStyle}" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,18,0,18">
            <Button Content="Open"
                    Command="{Binding OpenProfileCommand}" />
            <Button Margin="12,0,0,0"
                    Content="Refresh"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Command="{Binding RefreshRecentCommand}" />
            <Button Margin="12,0,0,0"
                    Content="Recent"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Command="{Binding OpenRecentProfileCommand}" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource SectionCardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="Saved"
                               Style="{StaticResource SectionHeaderTextStyle}" />
                    <ListBox Grid.Row="1"
                             Margin="0,14,0,0"
                             ItemsSource="{Binding RecentProfiles}"
                             SelectedItem="{Binding SelectedRecentProfile}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           TextWrapping="Wrap" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <StackPanel Grid.Column="2">
                <Border Style="{StaticResource SectionCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Selected"
                                   Style="{StaticResource SectionHeaderTextStyle}" />
                        <TextBlock Margin="0,14,0,0"
                                   Text="{Binding ProfileSummary}"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource SectionCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Categories"
                                   Style="{StaticResource SectionHeaderTextStyle}" />
                        <TextBlock Margin="0,14,0,0"
                                   Text="{Binding IncludedCategories}"
                                   TextWrapping="Wrap"
                                   Style="{StaticResource MutedLabelTextStyle}" />
                    </StackPanel>
                </Border>

            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
